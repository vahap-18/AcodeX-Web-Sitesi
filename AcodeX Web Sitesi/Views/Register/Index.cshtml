@*
  Kullanıcı kayıt sayfası
*@

@model EntityLayer.Concrate.Writer;

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/UserLayout.cshtml";
}

<!DOCTYPE html>
<html>
<head>
    <link href="~/tema/assets/css/stylesheet.css" rel="stylesheet" />
    <link href="~/tema/assets/css/style-starter.css" rel="stylesheet" />
    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
    <script src="~/lib/jquery/dist/jquery.js"></script>
    <title>Kayıt Ol</title>
</head>

<body>
  
    <div class="main-container">
        <div class="login-container" style="background-color:var(--bg-color); margin-top:50px;">
            <form method="post">
                <h4 style="text-align:center;">Kayıt Ol</h4>
                <div class="input-group">
                    <label for="name">İsim</label>
                    <input type="text" id="username" name="Name" placeholder="AcodeX" required>
                </div>
                <div class="input-group">
                    <label for="surname">Soyisim</label>
                    <input type="text" id="username" name="Surname" placeholder="Bilinmeyen bir kod" required>
                </div>
                <div class="input-group">
                    <label for="amail">E-mail</label>
                    <input type="email" id="username" name="Email" placeholder="acodex@gmail.com" required>
                </div>
                <div class="input-group">
                    <label for="password">Parola</label>
                    <input type="password" id="password" name="Password" placeholder="******" required>
                </div>

                <div class="form-check-mb-2">
                    <input type="checkbox" id="checkSart" class="form-check-input" id="checkSart" />
                    <label for="checkSart" class="form-check-label" id="SartKabul">Şartları kabul ediyorum</label>
                </div>

                <br />

                <button type="submit" id="kayitOl" disabled>Kayıt Ol</button> <br />
                <a class="nav-link" style="text-align:center; text-decoration:none;" asp-area="" asp-controller="Login" asp-action="Index">Giriş Yap </a>

                <div id="sart" style="display:none">
                    <b>Bu belge, EğitimSitesi.com web sitesine üye olurken kabul etmeniz gereken şartlar ve koşulları içermektedir.</b>

                    <h2>1. Kullanıcı Hesabı Oluşturma</h2>
                    <p>
                        1.1. EğitimSitesi.com'a üye olmak için 18 yaşını doldurmuş olmalısınız.

                        1.2. Hesap oluştururken sağladığınız bilgilerin doğru ve güncel olduğundan emin olmalısınız.

                        1.3. Hesabınızın güvenliği sizin sorumluluğunuzdadır. Hesabınızı başkalarıyla paylaşmaktan kaçınmalısınız.
                    </p>

                    <h2>
                        2. İçerik Kullanımı ve Paylaşımı
                    </h2>
                    <p>
                        2.1. EğitimSitesi.com'da paylaşılan içerikler sadece kişisel kullanım içindir. İzinsiz olarak içerikleri çoğaltmak, dağıtmak veya satmak yasaktır.

                        2.2. EğitimSitesi.com'da paylaşılan içeriklerde yer alan bilgilerin doğruluğunu sağlamak sizin sorumluluğunuzdadır.
                    </p>
                    <h2>
                        3. Yasaklı Faaliyetler
                    </h2>
                    <p>
                        3.1. EğitimSitesi.com'da aşağıdaki faaliyetler yasaktır:

                        - İçeriklere zarar vermek veya izinsiz olarak değiştirmek,
                        - Diğer kullanıcıların gizlilik haklarına tecavüz etmek,
                        - Yasadışı faaliyetlerde bulunmak veya teşvik etmek.
                    </p>
                    <h2>
                        4. Gizlilik Politikası
                    </h2>
                    <p>
                        4.1. EğitimSitesi.com'un gizlilik politikasını kabul ediyorsunuz. Daha fazla bilgi için Gizlilik Politikası sayfasını ziyaret edebilirsiniz.
                    </p>

                    <h2>
                        5. Değişiklikler
                    </h2>
                    <p> 5.1. EğitimSitesi.com, bu şartları zaman zaman güncelleme hakkını saklı tutar. Güncellenmiş şartlar yayınlandıktan sonra, EğitimSitesi.com'u kullanmaya devam etmek, güncellenmiş şartları kabul ettiğiniz anlamına gelir."</p>
                </div>

            </form>
        </div>
    </div>

    <button onclick="topFunction()" id="movetop" title="Go to top">
        <span class="fa fa-level-up" aria-hidden="true"></span>
    </button>


    <script>
        // Dropdown menülerin üzerine gelindiğinde açılmasını sağlayan fonksiyon
        function openDropdown() {
            var dropdownMenu = this.querySelector('.dropdown-menu');
            dropdownMenu.style.display = 'block';
        }

        // Dropdown menülerden çıkıldığında kapanmasını sağlayan fonksiyon
        function closeDropdown() {
            var dropdownMenu = this.querySelector('.dropdown-menu');
            dropdownMenu.style.display = 'none';
        }

        // Navbar içerisindeki tüm dropdown menülere event listener ekleyerek işlemi gerçekleştiriyoruz
        var dropdowns = document.querySelectorAll('.nav-item.dropdown');
        dropdowns.forEach(function (dropdown) {
            dropdown.addEventListener('mouseenter', openDropdown);
            dropdown.addEventListener('mouseleave', closeDropdown);
        });
    </script>

    <script>
        // Kullanıcı belgeden en üst 20 piksel aşağı kaydırdığında, düğmeyi göster
        window.onscroll = function () {
            kontrolEt();
        };

        function kontrolEt() {
            if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
                // Sayfa 20 piksel veya daha fazla aşağı kaydırıldıysa, düğmeyi görünür yap
                document.getElementById("movetop").style.display = "block";
            } else {
                // Aksi halde düğmeyi gizle
                document.getElementById("movetop").style.display = "none";
            }
        }

        // Düğmeye tıklandığında, belgenin en üstüne kaydır
        function enUsteGit() {
            document.body.scrollTop = 0;
            document.documentElement.scrollTop = 0;
        }
    </script>
    <script>
        // Belirli bir düğmeye tıklandığında dropdown menülerin açılmasını veya kapanmasını sağlayan fonksiyon
        function toggleDropdown() {
            var dropdownMenu = document.querySelector('.dropdown-menu');
            dropdownMenu.classList.toggle('show'); // 'show' sınıfını ekleyerek veya kaldırarak dropdown menünün görünürlüğünü değiştirir
        }
    </script>

    <script src="~/tema/assets/js/jquery-3.3.1.min.js"></script>
    <script src="~/tema/assets/js/theme-change.js"></script>
    <script>
        function autoType(elementClass, typingSpeed) {
            // Elementi seç ve stile göre düzenle
            var thhis = $(elementClass);
            thhis.css({
                "position": "relative",
                "display": "inline-block"
            });

            // Cursörü ekle
            thhis.prepend('<div class="cursor" style="right: initial; left:0;"></div>');

            // Text içeriğini al ve boşlukları temizle
            thhis = thhis.find(".text-js");
            var text = thhis.text().trim().split('');
            var amntOfChars = text.length;
            var newString = "";

            // Cursörü göster ve başlangıçtaki "|" karakterini kaldır
            thhis.text("|");
            setTimeout(function () {
                thhis.css("opacity", 1);
                thhis.prev().removeAttr("style");
                thhis.text("");

                // Her bir karakteri belirli bir hızda ekleyerek metni oluştur
                for (var i = 0; i < amntOfChars; i++) {
                    (function (i, char) {
                        setTimeout(function () {
                            newString += char;
                            thhis.text(newString);
                        }, i * typingSpeed);
                    })(i + 1, text[i]);
                }
            }, 1500); // Animasyonun başlangıç gecikmesi
        }

        // Sayfa yüklendiğinde çalışacak olan fonksiyon
        $(document).ready(function () {
            // autoType fonksiyonunu çağır ve hızı belirt
            autoType(".type-js", 200);
        });
    </script>


    <script src="~/tema/assets/js/jquery.magnific-popup.min.js"></script>
    <script>
        $(document).ready(function () {
            // Zoom animasyonlu popup için ayarları belirle
            $('.popup-with-zoom-anim').magnificPopup({
                type: 'inline',
                fixedContentPos: false,
                fixedBgPos: true,
                overflowY: 'auto',
                closeBtnInside: true,
                preloader: false,
                midClick: true,
                removalDelay: 300,
                mainClass: 'my-mfp-zoom-in' // Zoom animasyonu sınıfı
            });

            // Move animasyonlu popup için ayarları belirle
            $('.popup-with-move-anim').magnificPopup({
                type: 'inline',
                fixedContentPos: false,
                fixedBgPos: true,
                overflowY: 'auto',
                closeBtnInside: true,
                preloader: false,
                midClick: true,
                removalDelay: 300,
                mainClass: 'my-mfp-slide-bottom' // Move animasyonu sınıfı
            });
        });
    </script>


    <script>
        // Sayfa kaydırıldığında tetiklenen olay
        $(window).on("scroll", function () {
            var scroll = $(window).scrollTop();

            // Sayfa yukarı kaydırıldığında belirli bir noktadan sonra sabit navigasyonu ekle
            if (scroll >= 80) {
                $("#site-header").addClass("nav-fixed");
            } else {
                $("#site-header").removeClass("nav-fixed");
            }
        });

        // Mobil menü düğmesine tıklandığında tetiklenen olay
        $(".navbar-toggler").on("click", function () {
            // Header elementine 'active' sınıfını ekle veya kaldır
            $("header").toggleClass("active");
        });

        // Sayfa yüklendiğinde veya yeniden boyutlandırıldığında tetiklenen olay
        $(document).on("ready", function () {
            // Eğer pencere genişliği 991 pikselden büyükse
            if ($(window).width() > 991) {
                // Header elementinden 'active' sınıfını kaldır
                $("header").removeClass("active");
            }
            // Pencere boyutu yeniden boyutlandırıldığında
            $(window).on("resize", function () {
                // Eğer pencere genişliği 991 pikselden büyükse
                if ($(window).width() > 991) {
                    // Header elementinden 'active' sınıfını kaldır
                    $("header").removeClass("active");
                }
            });
        });
    </script>


    <script src="~/tema/assets/js/owl.carousel.js"></script>
    <script>
        $(document).ready(function () {
            // Owl Carousel'u başlat
            $("#owl-demo1").owlCarousel({
                loop: true, // Carousel döngüsü etkin
                margin: 20, // Her öğe arasındaki kenar boşluğu
                responsiveClass: true, // Duyarlı sınıfı etkinleştir
                responsive: {
                    // Ekran boyutlarına göre görüntülenen öğe sayısını ve navigasyon durumunu ayarla
                    0: {
                        items: 1, // 0 - 600 piksel aralığında, tek öğe göster
                        nav: true // Navigasyon düğmelerini göster
                    },
                    600: {
                        items: 1, // 600 - 1000 piksel aralığında, tek öğe göster
                        nav: false // Navigasyon düğmelerini gizle
                    },
                    1000: {
                        items: 1, // 1000 pikselden büyükse, tek öğe göster
                        nav: true, // Navigasyon düğmelerini göster
                        loop: true // Döngüyü etkinleştir
                    }
                }
            });
        });
    </script>

    <script>
        $(function () {
            $('.navbar-toggler').click(function () {
                $('body').toggleClass('noscroll');
            })
        });
    </script>
    <script src="~/tema/assets/js/bootstrap.min.js"></script>


    <script>

        // Paragraf elementini seç
        var paragraph = document.getElementById("sart");

        // Paragrafın içeriğini al
        var metin = paragraph.textContent;

        $('#SartKabul').click(function () {
            swal({
                title: "Kullanım koşulları",
                text: metin,
                icon: "info",
                buttons: true,
                dangerMode: true,
            })
                .then((willDelete) => {
                    if (willDelete) {
                        swal("Kullanım koşullarını kabul ettiniz", {
                            icon: "success",
                        });
                    } else {
                        swal("Kayıt olmak için kullanım koşullarını kabul etmek zorundasınız!", {
                            icon: "warning",
                        });
                    }
                });
        })
        // "Şartları kabul et" butonuna tıklanınca
        $('#checkSart').click(function () {
            // Checkbox'ı işaretle
            $('#checkSart').prop('checked', true);
            // Kayıt ol butonunu etkinleştir
            $('#kayitOl').prop('disabled', false);
        });

    </script>
</body>
</html>
